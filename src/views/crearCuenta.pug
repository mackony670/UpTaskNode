<!DOCTYPE html>
html(lang="en")
    head
        include parcials/header
   
    body.login
        .contenedor-formulario
            h1 Crea tu cuenta en 
                strong UpTask
            if errores
                each error in errores
                    .alerta.error= error.texto
            
            form#formulario.caja.caja-login(method="POST" action="/crear-cuenta")
                .campo
                    label(for='email') E-mail
                    input(type="email" name="email" placeholder="Coreo@coreo.com" required vlaue= email )
                    
                .campo
                    label(for='pasword') password
                    input(type="password" name="password" placeholder="Tu clave" required value= password )
                .campo
                    label(for='pasword') password
                    input(type="password" name="REpasword"  placeholder="repite Tu clave" required value= REpassword )
                .campo.enviar
                    input.boton(type="submit" value="Crear Cuenta") 
                .campo
                    a(href="/iniciar-sesion") Iniciar Sesion
        script(src="/dist/bundle.js")